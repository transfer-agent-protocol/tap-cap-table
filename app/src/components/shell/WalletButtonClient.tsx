/**
 * SSR-safe wallet entry for the shell. Dynamic import keeps connectors client-only.
 * Re-exports the native WalletButton from components/wallet.
 */
export { default } from "../wallet/WalletButton";
