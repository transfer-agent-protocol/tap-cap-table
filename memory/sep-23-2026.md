# Receipt-gated register — left off 2026-09-23

This change:

- Stock class and shareholder `/register-onchain` runs after the wallet receipt. The optimistic row is patched by id. A second submit is ignored while that write is pending, and the Add button stays disabled.
- If register fails, the modal says the wallet transaction succeeded and offers Save record. A stock class is not marked ready to issue until that save succeeds. The poller only updates an existing class or person.
- Issuance `register-onchain` only validates. It runs before `issueStock`. Transfer stays poller-only. The four write hooks stay.
- There is no 90s TTL. Session rows live in the dashboard until it unmounts.
- `validateInputAgainstSchema.js` resolves `ocf/schema` from `import.meta.url` and walks `items.allOf` / `items.anyOf` / `items.oneOf`. OCF pin stays `6d8c9322`. Do not bump it until vesting and equity-compensation dispatchers are smoked (#296).
- Agent docs no longer describe zip import, `chain/script/`, or `validateInputAgainstSchema(data, "Stakeholder", "object")`. Call `validateInputAgainstOCF(data, schemaObject)`.
- Port recovery is `docker compose stop`. `scripts/dev.sh --down` stops containers. `pnpm docker:down` still removes them. Do not use it.
- Closed #263 (server scale/UUID already go through `@tap/units`) and #262. #221 stays open: `mintSharesAuthorized` / `mintActivePositions` exist; the zip upload does not.

Still true from `memory/sep-7-2026.md` and `memory/sep-12-2026.md`: keep `/create`, `acceptStock`, `SAMPLE_ISSUER`, landing impl links; no beacon upgrade; do not hand-edit `generated.ts`.

Stack left running: rebuilt app and server images (`docker compose up -d --build server app`). Mongo container was not removed. UI http://localhost:3000/app, API http://localhost:8293, Mongo localhost:27027. Both app and API returned 200. Wallet confirmation of Save record was not run.

Next: user tests class/shareholder save with a browser wallet on Plume. #296 is still the OCF pin bump.
